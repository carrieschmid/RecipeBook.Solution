@{
  Layout = "_Layout";
}

@model RecipeBook.Models.Ingredient

<h2>Item Details</h2>
<hr>

<h3>Ingredient: @Html.DisplayFor(model => model.Name)</h3>

@if(@Model.RecipeIngredients.Count == 0)
{
  <p>This item does not belong to any recipes</p>
}
else
{
    <p>nothing here</p>
  @*<h4>Recipes the ingredient belongs to:</h4>
  <ul>
  @foreach(var join in Model.RecipeIngredients)
  {
    <li>@join.Name</li>
     @using (Html.BeginForm("DeleteRecipe", "Ingredient"))
    {
      @Html.Hidden("joinId", @join.RecipeIngredientId)
      <input type="submit" value="Delete"/>
    }
  }
  </ul>*@
}
